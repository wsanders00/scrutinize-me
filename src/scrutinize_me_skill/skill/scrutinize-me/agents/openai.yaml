interface:
  display_name: "Scrutinize Me"
  short_description: "Parallel, risk-first code review"
  default_prompt: "Use $scrutinize-me to review this change with specialized reviewers and return the final raw JSON review object matching the output schema."
policy:
  allow_implicit_invocation: true
